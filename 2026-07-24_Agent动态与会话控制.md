# SPEC：Agent 动态与会话控制

## 1. 文档定位

- 日期：2026-07-24
- 状态：待实施
- 适用范围：Orca 右侧 AI Vault / 会话历史顶部的 Agent 状态总览与会话控制
- 关联文档：`2026-07-23_agent-sidebar-conclusions.md`
- 替代范围：本文替代关联文档中的“右侧结论小框”、`agent-conclusions` 数据模型、相关交互和测试方案；左侧 live Agent 自动展开、`codexdb` / `codexdba` 图标区分仍按原文执行

本文不是对旧“Recent agent conclusions”组件做增量补丁，而是把右侧区域升级为统一的“Agent 动态”。实现必须先建立可靠、互斥的当前 Agent 活动模型，再展示最近完成记录。

## 2. 背景与问题

当前未提交实现只能从 `agentStatusByPaneKey` 和 `retainedAgentsByPaneKey` 中提取正常 `done` 且带 `lastAssistantMessage` 的记录，最多显示最近 3 条。该实现存在以下根本问题：

1. `working`、`waiting`、`blocked` 不会进入右侧区域。
2. 无法识别“Agent TUI 仍开着，但当前没有执行任务”的 idle 状态。
3. 未使用左侧 Agent 行已有的前台进程、live PTY 和标题证据。
4. 行点击只能展开 Markdown，不能回到当前 Agent。
5. 完成项没有按可用性提供打开原 Pane 或会话日志的动作。
6. 标题计数发生在 `.slice(0, 3)` 之后，不能代表完整活动状态。
7. live、retained、Provider Session 和 Pane 复用之间没有形成明确的互斥、去重和导航安全契约。
8. 新增代码触发多个 `max-lines` lint 错误，不能通过禁用规则规避。

因此，本功能必须从“完成结论列表”改为“当前活动 + 最近完成”的统一视图。

## 3. 目标

### 3.1 产品目标

用户打开会话历史时，应在一个区域内回答以下问题：

- 哪些 Agent 正在工作？
- 哪些 Agent 正在等待我处理？
- 哪些 Agent 已经打开，但当前空闲，可以继续派任务？
- 它们最近做到哪里、最近给出了什么结论？
- 我如何一键回到对应 Agent？
- 哪些 Agent 已结束，但仍有最近结论可以查看？

### 3.2 功能目标

1. 展示互斥的 `attention`、`working`、`idle`、`completed` 四类 Agent 动态。
2. 使用与左侧 Agent 行相同的状态、前台进程、PTY、标题和 Pane 归属证据。
3. 当前 Agent 与历史完成记录不重复。
4. 当前 Agent 可安全回到所属 worktree、tab 和 Pane。
5. 最近完成项可展开、复制，并在可用时打开会话日志或原 Pane。
6. 跟随 AI Vault 当前 workspace/project、execution host、Agent 类型和搜索条件。
7. 支持本机、SSH、WSL 和 runtime host，不假设本地路径或本地进程一定存在。
8. 所有新增和扩展文件通过现有 `max-lines`、类型、lint 和测试门禁。

## 4. 非目标

- 本次不加入 Stop、Kill、Interrupt、关闭 Pane 等破坏性操作。
- 本次不允许直接在 Agent 动态列表中输入或发送 Prompt；点击 idle Agent 后聚焦原 Pane，由用户在现有终端中继续输入。
- 不把 `retained done` 当成“Agent 仍开着”的证据。
- 不为缺少 Provider Session 身份的历史项猜测原 Pane。
- 不重新设计 AI Vault 的扫描、索引和虚拟列表。
- 不把所有历史 Vault Session 重新分类为 `codexdb` / `codexdba`。
- 不把 folder workspace 加入 Agent Dashboard；本次只让右侧 Agent 动态覆盖 folder workspace。
- 不新增独立的颜色体系、阴影层级或自定义无障碍语义。

## 5. 术语

### 5.1 当前 Agent

当前 Agent 是仍有可靠“正在工作”或“Agent TUI 仍存在”证据的 Agent。可靠证据包括：

- 新鲜的 hook 状态：`working`、`waiting`、`blocked`。
- `paneForegroundAgentByPaneKey[paneKey].agent` 非空且 `shellForeground === false`。
- live PTY 上可归属到 Agent 的 working / permission / idle 标题。

以下信息不能单独证明 Agent 当前仍存在：

- `retainedAgentsByPaneKey`。
- `entry.state === 'done'`。
- 超过 freshness 阈值的 `working` / `waiting` / `blocked`。
- 已睡眠、已关闭或仅保留旧 layout 的 Pane。
- 没有 live PTY 的旧标题。

### 5.2 历史完成

历史完成是正常 `done`、非 `interrupted`、带非空 `lastAssistantMessage`，但不再满足“当前 Agent”条件的记录。

### 5.3 idle

idle 表示 Agent TUI 仍存在，但当前没有执行任务。idle 必须同时满足：

1. 存在前台 Agent 或可归属的 live PTY idle 标题证据。
2. 不存在更新鲜的 `working`、`waiting` 或 `blocked` 状态。

`done` 只表示最近一轮完成；是否 idle 仍需当前存在证据。

## 6. 权威数据源

### 6.1 已有权威链路

左侧和 Agent Dashboard 已有以下可复用链路：

- `src/renderer/src/components/sidebar/worktree-agent-rows.ts`
  - `buildWorktreeAgentRows()`
- `src/renderer/src/components/sidebar/worktree-title-derived-agent-rows.ts`
  - live PTY、标题和前台 Agent 的 fallback 行
- `src/renderer/src/store/slices/pane-foreground-agent.ts`
  - `agent`
  - `routingTrusted`
  - `shellForeground`
- `src/renderer/src/components/sidebar/worktree-agent-row-selectors.ts`
- `src/renderer/src/components/dashboard/build-dashboard-snapshot.ts`
  - 已复用 `buildWorktreeAgentRows()` 生成跨 worktree 卡片
- `src/renderer/src/lib/pane-agent-evidence.ts`
  - freshness 和标题状态分类

### 6.2 当前缺口

`buildDashboardSnapshot()` 当前未将 `paneForegroundAgentByPaneKey` 传入 `buildWorktreeAgentRows()`，并将 live `done`、真实 idle、stale 状态和 retained `done` 折叠到同一个 `idle` bucket，不能直接满足本文需求。

实现应扩展这条共享链路，不得在 `agent-conclusions.ts` 中另写一套 idle 推断。

### 6.3 证据来源模型

共享派生层必须显式保留证据来源，不能继续依赖 `startedAt === 0` 等间接哨兵判断内容是否为合成值。

建议增加：

```ts
type AgentPresenceEvidence =
  | 'fresh-hook'
  | 'foreground-process'
  | 'live-title'
  | 'stale-hook'
  | 'retained'

type AgentContentEvidence = 'provider' | 'synthetic'
```

要求：

- `fresh-hook` 可证明 attention / working。
- `foreground-process` 和 `live-title` 可证明 Agent TUI 当前存在。
- `stale-hook` 不能单独证明当前存在。
- `retained` 只能用于历史完成。
- `synthetic` 内容不能作为真实 Prompt、工具步骤或最近结论显示。

如果实现者采用不同命名，必须保留相同语义和测试边界。

### 6.4 不可变 Agent lifecycle identity

`stateHistory` 受 `AGENT_STATE_HISTORY_MAX` 限制并会裁剪，最早的 `stateHistory.startedAt` 会随长期运行和频繁状态切换而变化，因此不能承担 Session 身份或安全导航校验。

必须增加独立且不随状态历史裁剪的生命周期身份：

```ts
type AgentLifecycleIdentity = {
  id: string
  startedAt: number
}
```

live Agent 状态、Pane Agent 生命周期记录和 retained 快照中保存：

```ts
agentLifecycleId: string
agentSessionStartedAt: number
```

其中：

- `agentLifecycleId` 是身份字段，必须使用新生成的不可预测 opaque ID，不能由时间戳单独充当。
- `agentSessionStartedAt` 只用于显示、排序和诊断，不作为唯一身份。
- `stateHistory`、`stateStartedAt`、`updatedAt` 都不能替代 `agentLifecycleId`。
- 同一 Agent TUI 内的 Prompt、tool、working/waiting/blocked/done/idle 状态切换不重置 lifecycle。
- `stateHistory` 裁剪不重置 lifecycle。
- 同一 lifecycle 后续获得 Provider Session ID 时，补充 Provider 身份但不更换 `agentLifecycleId`。

lifecycle 必须在以下事件发生时结束；之后再次识别到 Agent 时创建新 identity：

- 已绑定的 PTY 被不同 PTY 替换。
- Pane authority 被 retire，Tab / Pane 关闭或 worktree teardown。
- `shellForeground === true` 确认已经回到 shell。
- Agent 类型变为不兼容的另一类型。
- 已有 Provider Session ID 变为另一个非空 ID。
- Orca launch token 明确变化。

`agent === null && shellForeground === false` 只表示探测结果不确定，不能据此结束 lifecycle。“前台 Agent 从无到有”只有在之前已经收到 confirmed shell、PTY replacement 或明确 retire 事件时才是新 lifecycle；短暂探测空窗不能产生新 ID。

本机前台进程、hook 状态和 title-derived / SSH Agent 都必须能获得 lifecycle identity。新增 `pane-agent-lifecycle.ts` 和 Zustand slice，`paneAgentLifecycleByPaneKey` 是 lifecycle identity 的唯一写入者；共享派生函数只读该 slice，禁止在 selector、row builder、Dashboard snapshot 或 AI Vault 模型中临时生成 ID。

建议记录：

```ts
type PaneAgentLifecycle = {
  id: string
  startedAt: number
  paneKey: string
  executionHostId: ExecutionHostId
  connectionId: string | null
  ptyId: string | null
  runtimeAgent: TuiAgent | null
  providerSessionId: string | null
  launchToken: string | null
  phase: 'active' | 'transport-disconnected'
  authorityRevision: number
}
```

每个 lifecycle 事件必须携带 `observedAt` 和当前可获得的 PTY / `authorityRevision`。reducer 必须幂等，并拒绝旧 revision 的迟到事件，避免旧 SSH 回调复活已 retire 或已被新 PTY 替换的 identity。

唯一写入者接收下列事件：

| 事件 | 保留 / 重置规则 |
|---|---|
| 首次 `agent-observed` | live Pane 上没有 lifecycle 时创建；同一 Pane、Host、PTY 和兼容 Agent 的重复事件保留 |
| `provider-session-observed` | 从空变为非空只补充字段；从一个非空 ID 变为另一个非空 ID 时结束旧 lifecycle 并创建新 lifecycle |
| `launch-token-observed` | 从空变为非空只补充字段；已有 token 明确变化时重置 |
| `pty-bound` | lifecycle 尚无 PTY 时补充；同一 PTY 重复绑定时保留 |
| `pty-replaced` | 已绑定 PTY 与新 PTY 不同时重置；不能把旧 retained identity 复制给新 PTY |
| `foreground-agent-observed` | 兼容 Agent 保留并补充；明确识别为不兼容 Agent 时重置 |
| `foreground-inconclusive` | 对应 `agent === null && shellForeground === false`；保留，不改变 phase |
| `shell-confirmed` | 对应 `shellForeground === true`；结束并移除 lifecycle，下一次 Agent 观察创建新 ID |
| `transport-disconnected` | SSH 短暂断连只标记 `transport-disconnected`；该记录不证明 current，也不可导航，但暂不更换 ID |
| `transport-reattached` | 只有 Host、connection、PTY / relay authority 和 Pane authority 均能证明连续时恢复同一 ID；PTY 被替换、Host 变化、证明缺失或保留期已过时结束旧 lifecycle |
| `authority-transferred` | `fromPaneKey → toPaneKey` 且 PTY / Host authority 连续时原样移动同一记录；目标已有冲突 lifecycle、PTY 不同或 Host 不同则不得合并，按新 authority 创建或等待下一次观察 |
| `pane-retired` | Tab / Pane 关闭、worktree teardown 或明确 authority retire 时结束并移除 |

事件接入边界：

- `setAgentStatus()`、前台进程 tracker、PTY bind / replacement、终端标题更新的事件入口、SSH connection clear / reconnect、`transferAgentPaneAuthority()` 和 `retireAgentPaneAuthority()` 只能向该 reducer 发送事件。
- `buildWorktreeAgentRows()`、title-derived row builder 和其他 selector 必须保持纯函数，只读取 reducer 已写入的 identity，绝不能在渲染派生期间 dispatch 或生成 ID。
- `setAgentStatus()` 获取 reducer 返回的当前 identity，再把 `agentLifecycleId` / `agentSessionStartedAt` 盖章到 live status；状态切换本身不生成 identity。
- working / permission / idle 标题切换、Prompt 和 tool 更新、`stateHistory` 裁剪均保留 lifecycle。
- `transferAgentPaneAuthority()` 必须与其他 pane-keyed map 一样原样移动 lifecycle；retire 必须同步清理。
- 应用重启后若没有可持久验证的 PTY / relay authority 连续性，保守地创建新 ID，不把旧 completed 绑定到新 current。

`retainedAgentsByPaneKey` 不是 lifecycle 写入者。生成 retained 快照时必须原样复制 live status 上的 `agentLifecycleId`、`agentSessionStartedAt` 和 `executionHostId`，不能补造、刷新或重新开始 identity。缺失 lifecycle 的旧 retained 数据只能显示为不可导航的 completed，不能参与 current 去重或 Pane 导航。

## 7. 状态判定

### 7.1 判定优先级

按以下顺序分类，每个来源记录最终只能进入一个类别：

1. `attention`
2. `working`
3. `idle`
4. `completed`
5. 不展示

### 7.2 判定表

| 条件 | 分类 | 说明 |
|---|---|---|
| 新鲜 `waiting` / `blocked` | `attention` | hook 状态本身可证明当前工作需要用户介入 |
| 新鲜 `working` | `working` | 不需要额外前台进程证据 |
| live Pane，前台 Agent 存在，当前为 `done` | `idle` | 最近一轮已完成，但 Agent TUI 仍开着 |
| live Pane，前台 Agent 存在，状态已 stale | `idle` | stale 状态不能继续显示 working，但前台 Agent 可证明仍开着 |
| live PTY 标题明确为 Agent idle | `idle` | 适用于 SSH 或无本地进程证据的场景 |
| live PTY 标题明确为 Agent working | `working` | title `working` 归一为展示 `working` |
| live PTY 标题明确为 Agent permission | `attention/waiting` | `permission` 必须统一映射为 `waiting`，不得映射为 `blocked` |
| `shellForeground === true` | 不得为 `idle` | Agent 已回到 shell |
| stale `working` 且无前台 Agent、无 live title 证据 | 不属于当前 Agent | 防止僵尸工作项 |
| 正常 `done`、有结论、且不属于当前 Agent | `completed` | 历史完成 |
| `interrupted === true` | 不属于 `completed` | 可以在其他现有 UI 中保留 interrupted 状态，本区域不作为成功结论 |
| retained `done` | 仅可能为 `completed` | 绝不能据此判定 idle |
| title-derived synthetic “Idle” | 可证明 idle，但不能当作最近结论 | 状态证据与内容证据分离 |

### 7.3 title 状态归一化

title-derived 原始状态只允许在共享证据层出现：

```ts
type TitleAgentActivity = 'working' | 'permission' | 'idle'
```

进入 `AgentActivitySource` 前必须统一映射：

| Title 原始状态 | `AgentActivitySource.state` | kind | 可见文案 | 状态标记 | 同类排序 |
|---|---|---|---|---|---|
| `working` | `working` | `working` | “运行中” | `AgentStateDot state="working"` | working 组 |
| `permission` | `waiting` | `attention` | “等待输入” | `AgentStateDot state="waiting"` | waiting 在 blocked 之前 |
| `idle` | `idle` | `idle` | “已打开 · 当前空闲” | `AgentStateDot state="idle"` | idle 组 |

`permission` 不得映射为 `blocked`。`blocked` 只来自明确的 hook / Provider 阻塞状态，继续使用红色 blocked 标记和“已阻塞”文案。

title-derived permission 没有真实 `interactivePrompt` 时，摘要显示中性的“等待输入”，不得把 synthetic title 或 Agent label 伪装成用户问题。

建议来源模型同时保留：

```ts
rawState: AgentStatusState | TitleAgentActivity
state: AgentStatusState | 'idle'
```

其中 `state` 始终是归一化后的展示和排序状态，UI 层不得再次解释 `permission`。

### 7.4 fresh 与 stale

- 复用 `AGENT_STATUS_STALE_AFTER_MS` 和 `isExplicitAgentStatusFresh()`。
- freshness 到期必须通过已有 `agentStatusEpoch` 触发重新派生。
- stale active 状态不可继续显示为 working / attention。
- stale 状态只有在存在 `foreground-process` 或 `live-title` 证据时才能降级为 idle。
- 如果没有当前存在证据，则从当前 Agent 列表移除。

### 7.5 idle 与 completed 的互斥

同一个 Provider Session / Pane 完成一轮后仍开着：

- 只生成一个 `idle` 项。
- idle 项可携带该 Session 最近一次真实 `lastAssistantMessage`。
- 不再额外生成 `completed` 项。

当 Agent 退出、Pane 关闭、PTY 消失或 `shellForeground === true`：

- idle 项消失。
- 若存在正常完成结论，则可以转为 `completed`。

## 8. 数据模型

### 8.1 当前活动来源

建议先生成与展示无关的来源模型：

```ts
type AgentActivitySource = {
  paneKey: string
  worktreeId: string
  executionHostId: ExecutionHostId
  runtimeAgent: TuiAgent | null
  vaultAgent: AiVaultAgent | null
  providerSessionId: string | null
  agentLifecycleId: string | null
  agentSessionStartedAt: number | null
  activityIdentity: AgentActivityIdentity | null
  rawState: AgentStatusState | TitleAgentActivity
  state: AgentStatusState | 'idle'
  rowSource: 'live' | 'retained' | 'title'
  presenceEvidence: AgentPresenceEvidence
  contentEvidence: AgentContentEvidence
  prompt: string | null
  lastAssistantMessage: string | null
  toolName: string | null
  toolInput: string | null
  interactivePrompt: string | null
  stateStartedAt: number
  updatedAt: number
  interrupted: boolean
}
```

要求：

- `agentLifecycleId` 来自 6.4 的独立 lifecycle store / status 字段，不得从 `stateHistory` 反推。
- `agentSessionStartedAt` 来自 lifecycle 创建时间，只负责显示和排序。
- `activityIdentity` 按 8.3 的统一规则生成，其 canonical key 和 aliases 集合用于 current/completed 互斥和去重。
- `rawState` 保留诊断来源；`state` 已完成 7.3 的统一归一化。
- 缺少 Provider Session 且缺少 `agentLifecycleId` 的旧 retained 来源，其 `activityIdentity` 为 `null`，只能作为不可导航的 completed 候选。

### 8.2 展示模型

```ts
type AgentActivityKind = 'attention' | 'working' | 'idle' | 'completed'

type AgentActivityItem = {
  id: string
  kind: AgentActivityKind
  state: 'working' | 'blocked' | 'waiting' | 'idle' | 'done'
  paneKey: string | null
  worktreeId: string | null
  executionHostId: ExecutionHostId
  runtimeAgent: TuiAgent | null
  vaultAgent: AiVaultAgent | null
  title: string
  subtitle: string | null
  message: string | null
  toolName: string | null
  toolInput: string | null
  interactivePrompt: string | null
  startedAt: number | null
  stateChangedAt: number
  updatedAt: number
  completedAt: number | null
  providerSessionId: string | null
  agentLifecycleId: string | null
  agentSessionStartedAt: number | null
  activityIdentity: AgentActivityIdentity | null
  matchedSession: AiVaultSession | null
  navigationTarget: AgentActivityNavigationTarget | null
  navigationUnavailableReason: AgentActivityNavigationUnavailableReason | null
}
```

### 8.3 稳定身份

用于互斥和去重的 Session identity 与用于 React 渲染和历史列表的 `item.id` 必须分离：

```ts
type AgentActivityIdentity = {
  canonicalKey: string
  aliases: ReadonlySet<string>
}
```

Provider Session 存在时：

```text
canonicalKey =
  encodeIdentityTuple("provider", [
    executionHostId,
    normalizedVaultAgent,
    providerSessionId,
  ])
```

Provider Session 不存在时：

```text
canonicalKey =
  encodeIdentityTuple("lifecycle", [
    executionHostId,
    worktreeId,
    paneKey,
    runtimeAgent,
    agentLifecycleId,
  ])
```

规则：

- canonical key 专门表示当前最强的 Agent lifecycle / Provider Session 身份。
- `aliases` 必须至少包含 canonical key；存在 lifecycle identity 时也必须包含 lifecycle key。
- 同一 lifecycle 后续获得 Provider Session ID 时，Provider key 成为 canonical key，原 lifecycle key 保留在 `aliases` 中。
- current 和 completed 必须以 aliases 集合是否相交做互斥，不能通过比较 `item.id` 去重。
- `agentLifecycleId` 缺失时禁止退回到 `stateHistory.startedAt`、`stateStartedAt`、`updatedAt` 或单独 `paneKey`。
- canonical key 与 aliases 的构建必须集中在 `agent-activity-identity.ts`，其他模块不得拼接私有 key。

所有 key 使用同一个带版本和类型标签的 NUL tuple 编码，例如：

```text
v1\0provider\0<executionHostId>\0<vaultAgent>\0<providerSessionId>
v1\0lifecycle\0<executionHostId>\0<worktreeId>\0<paneKey>\0<runtimeAgent>\0<agentLifecycleId>
```

`encodeIdentityTuple()` 必须拒绝包含 `\0` 的组件；禁止用无分隔符模板字符串或可产生碰撞的自由文本拼接。该编码同时用于 10.1 的 Provider 三元组索引。

展示 item ID：

```text
current item id =
  encodeDisplayItemId(["current", activityIdentity.canonicalKey])

completed item id =
  encodeDisplayItemId([
    "completed",
    activityIdentity.canonicalKey,
    completedAt,
  ])
```

`encodeDisplayItemId()` 使用结构化 JSON tuple 或长度前缀编码，可把已经包含 NUL 的 canonical key 当作一个完整字段；它不参与 Session 匹配。不得把 canonical key 再作为 `encodeIdentityTuple()` 的普通组件，否则会违反 NUL 输入校验。

没有 `activityIdentity` 的兼容性 completed item 可使用独立的 legacy history ID，但必须：

- 标记为历史且不可导航。
- 不参与 current 抑制。
- 不与任何当前 Pane 建立关系。

## 9. 共享派生架构

### 9.1 当前 Agent 行仍以左侧逻辑为权威

`buildWorktreeAgentRows()` 继续作为具体 workspace 内的权威 Agent 行构建器，但需补齐或抽取“当前存在证据”：

- fresh hook。
- pane foreground Agent。
- live PTY title。
- shell foreground 排除。
- stale hook 不足以证明当前存在。

左侧列表、Agent Dashboard 和右侧 Agent 动态必须消费同一纯函数结果或同一证据派生函数。

共享派生层还必须接收 `paneAgentLifecycleByPaneKey`，把不可变 lifecycle identity 附着到 hook、foreground 和 title-derived 行。状态行构建不得从有限长度的 `stateHistory` 重新计算 lifecycle。

### 9.2 共享行派生与 Dashboard snapshot

`buildDashboardSnapshot()` 已经复用左侧行构建器，应优先接入共享证据，而不是新建平行状态系统：

1. `DashboardSnapshotState` 增加 `paneForegroundAgentByPaneKey`。
2. `DashboardSnapshotState` 增加 `paneAgentLifecycleByPaneKey`。
3. 通过 `selectPaneForegroundAgentsForWorktree()` 将数据传给 `buildWorktreeAgentRows()`。
4. 通过具体命名 selector 将 lifecycle identity 传给共享行构建器。
5. `DashboardCard` 增加明确的 `rowSource` / `presenceEvidence` / `contentEvidence` / `agentLifecycleId`，区分当前与 retained。
6. 不再仅用 `bucket === 'idle'` 判断当前 idle。
7. `useAgentBucketCounts()` 同步订阅 `paneForegroundAgentByPaneKey` 和 lifecycle map。
8. Agent Dashboard 现有 UI 语义如不需要区分历史完成，可继续把完成项放在 Idle 列，但共享数据必须保留精确来源。

本次不改变 `DashboardCard` 的 repo contract，也不让 folder workspace 进入 Dashboard：

- `DashboardCard.repoId` / `repoName` 继续保持必填。
- 不得为 folder workspace 伪造 repo ID、repoName 或空字符串占位。
- Dashboard 只消费现有 repo worktree 的共享状态证据。
- 若未来需要在 Dashboard 展示 folder workspace，应单独把卡片模型改为明确的 repo / folder discriminated union，不属于本 SPEC 范围。

### 9.3 folder workspace

右侧 Agent 动态不能只遍历 repo worktree。实现必须覆盖：

- 普通 worktree。
- folder workspace。
- runtime-owned folder workspace。

建议抽取一个具体命名的 workspace 迭代层，例如：

```ts
type AgentActivityWorkspaceDescriptor = {
  id: string
  title: string
  projectKey: string | null
  executionHostId: ExecutionHostId
}
```

它负责把 `tabsByWorktree[workspaceId]`、retained attribution、Host 和 Project 信息交给同一行构建器。不要在 folder workspace 分支另写状态规则。该 descriptor 仅服务共享行派生和右侧 Agent 动态，不要求把 folder workspace 转换成 `DashboardCard`。

### 9.4 AI Vault 展示派生

新增具体命名模块，建议：

- `src/renderer/src/components/right-sidebar/agent-activity-model.ts`
- `src/renderer/src/components/right-sidebar/agent-activity-filters.ts`
- `src/renderer/src/components/right-sidebar/agent-activity-identity.ts`

职责：

1. 接收共享当前 Agent 来源。
2. 匹配 AI Vault Session。
3. 先生成并索引 current `attention` / `working` / `idle`。
4. 再从 live done 和 retained done 生成 `completed`。
5. 按 Provider Session 和 Pane 身份抑制重复 completed。
6. 应用 scope、host、Agent 类型和搜索过滤。
7. 输出完整计数和展示列表；不能在计数前截断。

旧 `agent-conclusions.ts` 应被替换或收敛为 completed 来源模块，不应继续承担全部 Agent 动态。

## 10. AI Vault Session 匹配

### 10.1 Agent 归一化

- `codex`、`codexdb`、`codexdba` 的 Vault Agent 均为 `codex`。
- UI 中保留真实 `runtimeAgent`，用于图标和标签。
- Provider Session 的精确身份固定为 `executionHostId + normalizedVaultAgent + providerSessionId` 三元组，三者缺一不可。
- `ai-vault-original-pane.ts` 和 Agent 动态必须复用同一个归一化函数。

```ts
type AgentProviderIdentity = {
  executionHostId: ExecutionHostId
  normalizedVaultAgent: AiVaultAgent
  providerSessionId: string
}
```

匹配、索引和导航校验统一使用 8.3 的 `encodeIdentityTuple("provider", ...)`。不得只比较 Provider Session ID，也不得把 Host 不明的记录默认归到 local host。

### 10.2 原 Pane 索引的 Host 隔离

`ai-vault-original-pane-index.ts` 当前仅以 `agent + sessionId` 建索引，必须改为 Provider 三元组 key：

```text
providerPaneIndex key =
  encodeIdentityTuple("provider", [
    executionHostId,
    normalizedVaultAgent,
    providerSessionId,
  ])
```

要求：

- live、retained、sleeping 三类索引项都必须携带或可唯一解析 `executionHostId`。
- Host 优先来自 workspace descriptor / worktree / folder workspace 的权威执行 Host；SSH status 的 connection 信息只能作为相同 Host 的佐证，不能把无法解析的 Host 猜为本机。
- prompt fallback 也必须限制在相同 `executionHostId + normalizedVaultAgent` 内；同 Host 仍有多个候选时返回 `null`。
- 本机与 SSH、两个 SSH Host 或 runtime host 上出现相同 Agent 和 Provider Session ID 时，必须分别索引，不能互相覆盖。
- Host 缺失、候选跨 Host 冲突或三元组不完整时，不生成可导航原 Pane。

### 10.3 搜索与过滤

Agent 动态跟随当前：

- `AiVaultScope`：workspace / project / all。
- `ExecutionHostScope`。
- 已启用 Agent 类型。
- 搜索条件。

规则：

- 已匹配 Session 的项，仅当 Session 位于 `filteredSessionIds` 中时展示。
- 搜索框非空时，未匹配 Session 的来源项不自行复制 AI Vault 全文搜索语义。
- 搜索框为空时，未匹配 Session 的当前项仍可按 workspace/project、host 和 Agent 类型展示。
- 标题统计与实际列表必须使用同一过滤结果。
- 被 idle 折叠或 completed 上限隐藏的项仍计入完整统计。

## 11. 去重与 Pane 复用

### 11.1 current 优先

构建顺序必须是：

1. current source index。
2. completed history index。

先为 current 和 completed 生成集中定义的 `AgentActivityIdentity`。若双方的 canonical key / aliases 集合相交：

- current 项保留。
- completed 项被抑制。
- 最近结论附着到 current idle 项。
- 比较发生在生成展示 `item.id` 和 completed 三条截断之前。
- 没有 `activityIdentity` 的 legacy completed 不得抑制任何 current 项。

### 11.2 Pane 被复用

Pane 被新 Agent 或新 Provider Session 复用时：

- 旧 retained 记录不能成为当前项。
- 旧 completed 可以保留为历史记录，但不得获得当前 Pane 导航目标。
- 新 current 项使用新的 Provider Session 或新的 `agentLifecycleId` 身份。
- 不能仅按 `paneKey` 合并不同 Provider Session。
- lifecycle 重置后，即使 Pane、Agent 类型相同，也必须生成不同 lifecycle key。

### 11.3 无 Provider Session

没有 Provider Session 时：

- 当前项导航只允许绑定当前 live Pane。
- 必须在点击时重新验证 `executionHostId`、`paneKey`、workspace、Agent 类型和不可变 `agentLifecycleId`。
- 历史 completed 不提供“打开原 Pane”，除非现有查找逻辑能给出额外的精确身份。
- 不允许通过相似 Prompt 把旧 completed 绑定到已复用 Pane。
- 缺少 `agentLifecycleId` 的当前来源不得生成可点击导航目标。

## 12. 安全导航

### 12.1 导航目标

```ts
type AgentActivityNavigationTarget = {
  worktreeId: string
  paneKey: string
  executionHostId: ExecutionHostId
  runtimeAgent: TuiAgent | null
  normalizedVaultAgent: AiVaultAgent | null
  providerSessionId: string | null
  agentLifecycleId: string
  activityIdentity: AgentActivityIdentity
}
```

`AiVaultOriginalPaneTarget` 同样必须增加 `executionHostId`。有 Provider Session 的 target 必须同时携带 `normalizedVaultAgent` 和 `providerSessionId`，不能让调用方只凭 session ID 二次查找。

### 12.2 点击 current 项

点击 `attention`、`working`、`idle` 行时：

1. 从 `useAppStore.getState()` 重新读取当前状态。
2. 验证 worktree / folder workspace 仍存在。
3. 重新解析该 workspace 的 `executionHostId`，要求与 target 完全相等；Host 缺失或变化时拒绝导航。
4. 验证 Pane key 可解析，Tab 仍属于该 workspace。
5. 验证 leaf 仍存在于当前 layout。
6. 有 Provider Session 时要求当前记录的 `executionHostId + normalizedVaultAgent + providerSessionId` 三元组与 target 完全相等。
7. 无 Provider Session 时要求 Host、Agent 类型、`agentLifecycleId` 和重新计算的 lifecycle canonical key 均精确匹配。
8. 对 idle 重新验证前台 Agent 或 live title 当前存在证据。
9. 验证通过后才调用 `activateAndRevealWorktree()` 和 `activateTabAndFocusPane()`。
10. 验证失败时显示中性错误提示，不跳转到可能已复用或属于另一 Host 的 Pane。

### 12.3 无可导航 Pane 的 current 项

current 状态的展示与可导航性分离。attention / working / idle 只要存在有效 current 证据就必须展示，即使 orchestration、缺失 Pane、身份歧义，或远程 Pane 断连但仍有独立 fresh current 证据，导致 `navigationTarget === null`。

```ts
type AgentActivityNavigationUnavailableReason =
  | 'host-unresolved'
  | 'pane-unavailable'
  | 'identity-ambiguous'
  | 'lifecycle-missing'
  | 'remote-disconnected'
```

规则：

- 无 target 的 current 行渲染为非交互容器，不得渲染成 button、不得绑定 click / keyboard handler、不得进入 tab order。
- 不显示伪造的“回到 Agent”按钮，也不使用 hover、chevron 或 cursor 暗示可以跳转。
- 行仍展示状态、摘要和 workspace；通过可见的弱提示或可访问说明表达“无法定位原 Pane”，并根据 `navigationUnavailableReason` 提供原因。
- 如果稍后证据恢复且能构造安全 target，下一次派生可恢复交互；UI 组件本身不得猜测 Pane。

### 12.4 completed 动作

- 展开：显示已捕获的 `lastAssistantMessage`。
- 复制：复制结论。
- 打开会话日志：仅当 `matchedSession` 存在且 `canOpenAiVaultSessionLogInOrca()` 为 true。
- 打开原 Pane：仅当点击时能通过 Host + normalized Agent + Provider Session 三元组校验。
- 无可用动作时不显示禁用图标堆叠。

## 13. UI 与交互

### 13.1 区域标题

标题：

```text
Agent 动态
```

右侧统计示例：

```text
1 待处理 · 2 运行 · 3 空闲
```

统计只显示非零项；状态必须有可见文字，不能只依赖颜色或圆点。

### 13.2 排序

固定优先级：

1. `waiting`
2. `blocked`
3. `working`
4. `idle`
5. `completed`

同类内：

- current 按 `stateChangedAt` 倒序。
- completed 按 `completedAt` 倒序。
- 时间相同按稳定 `id` 升序。

### 13.3 行内容

每个 current 行展示：

- Agent 图标和名称。
- `AgentStateDot`。
- 明确状态文字。
- worktree / folder workspace 或 orchestration task 标题。
- 状态持续时间或最后更新时间。
- 一行真实摘要。

摘要优先级：

- `working`：`toolName + toolInput`，没有时使用真实 Prompt 或最近 Provider 消息。
- `waiting` / `blocked`：`interactivePrompt` 的安全摘要，其次是真实 Provider 消息。
- `idle`：最近真实 `lastAssistantMessage`；没有时显示“已打开，当前空闲”。
- `completed`：最后结论。

title-derived synthetic `Idle`、`Running`、Agent label 等状态词不得伪装成 Provider 消息。

### 13.4 展开与数量

- attention 和 working 全部直接显示，不得截断。
- idle 默认显示前 3 条。
- 超过 3 条时显示“还有 N 个空闲 Agent”，支持展开和收起。
- completed 最多显示最近 3 条。
- current 不占 completed 的 3 条额度。
- 不使用嵌套滚动条；区域高度通过 idle 展开/收起控制。

### 13.5 点击语义

- `navigationTarget` 存在的 current 行主体：聚焦 Agent Pane。
- `navigationTarget === null` 的 current 行：保留状态内容，但按 12.3 渲染为不可交互内容，并说明无法定位原 Pane 的原因。
- current 行的可选 disclosure：仅展开额外状态内容，不能抢占主体导航。
- completed 行主体：展开/收起结论。
- Copy、Open log、Open pane 使用独立可访问按钮和 Tooltip。
- 键盘可聚焦；Enter 执行主体动作，Space 保持按钮语义。

### 13.6 视觉约束

- 遵守 `docs/STYLEGUIDE.md`。
- 使用 `main.css` token、现有 sidebar token 和 shadcn primitive。
- 状态复用 `AgentStateDot`。
- 图标来自 Lucide 或现有 `AgentIcon`。
- 不硬编码新颜色。
- 不新增第四层阴影。
- 支持浅色、深色、macOS、Windows、Linux。
- 动画遵守 reduced-motion。

## 14. 组件和文件拆分

建议组件：

- `pane-agent-lifecycle.ts`
  - lifecycle slice、唯一事件 reducer、创建/保留/转移/重置不可变 identity。
- `agent-activity-identity.ts`
  - NUL tuple 编码、Provider 三元组 key、lifecycle key、canonical key 和 aliases 集合。
- `ai-vault-original-pane-index.ts`
  - 将原 Pane 索引升级为 Host + normalized Agent + Provider Session 三元组。
- `AgentActivityBox.tsx`
  - 仅负责区域结构和折叠状态。
- `AgentActivityCurrentRow.tsx`
  - attention / working / idle。
- `AgentActivityCompletedRow.tsx`
  - completed 展开和动作。
- `useAiVaultAgentActivity.ts`
  - 订阅 store、构建 workspace 描述、调用纯模型。
- `agent-activity-model.ts`
  - 分类、互斥和排序。
- `agent-activity-filters.ts`
  - scope / host / Agent / search。
- `agent-activity-navigation.ts`
  - 点击时二次验证和导航。

最终文件名可调整，但每个文件必须职责具体，不得使用 `utils`、`helpers`、`common` 等模糊命名。

`AiVaultPanel.tsx` 只保留组合逻辑：

```tsx
const activity = useAiVaultAgentActivity(...)
<AgentActivityBox model={activity} ... />
```

不得继续把完整 workspace 映射、状态派生、过滤和导航逻辑堆入 `AiVaultPanel.tsx`。

## 15. max-lines 修复

禁止添加任何 `max-lines` disable 或 per-file 上限。

本功能相关文件按具体职责拆分：

- `AiVaultPanel.tsx`
  - 抽出 `useAiVaultAgentActivity.ts` 和 workspace scope 构建。
- `agent-catalog.tsx`
  - 抽出 `CodexProfileIcon` 到 `codex-profile-icon.tsx`。
  - 如仍超限，将本地 wrapper catalog entries 抽到 `local-wrapper-agent-catalog.ts`。
- `ai-vault-types.ts`
  - 将 `toAiVaultAgent()`、`aiVaultAgentMatchesRuntimeAgent()` 抽到 `ai-vault-agent-normalization.ts`。
- `tui-agent-config.ts`
  - 将 `cc-*`、`codexdb`、`codexdba` 配置抽到 `tui-wrapper-agent-config.ts`，再通过明确导出合并。
- `host-section-rows.test.ts`
  - 将 runtime/folder workspace Host 场景拆到 `host-section-folder-workspace-rows.test.ts`。

拆分后必须保持公开 API 具体、无循环依赖，并补齐拆分模块测试。

## 16. 性能约束

- 不允许在每个渲染行内扫描全部 Agent、Tab 或 Session。
- workspace、tab、pane、provider-session 索引在一次派生中构建一次。
- 继续使用 selector 的引用复用策略，避免无关 Agent ping 触发所有行重渲染。
- `Date.now()` 相关变化通过现有 `agentStatusEpoch` 驱动。
- 对 AI Vault Session 的匹配使用 Map 索引，不做 current × sessions 的嵌套扫描。
- 远程状态延迟不能造成列表焦点跳动。

## 17. 测试计划

### 17.1 共享状态派生

至少覆盖：

- fresh working → `working`。
- fresh waiting → `attention/waiting`。
- fresh blocked → `attention/blocked`。
- title permission → `waiting`，不得变为 `blocked`。
- title permission 的文案、状态点和排序统一使用 waiting 语义。
- live `done` + foreground Agent + `shellForeground === false` → `idle`。
- stale working + foreground Agent → `idle`。
- stale working + 无 foreground / live title → 不属于 current。
- live PTY idle title → `idle`。
- live PTY working title → `working`。
- live PTY permission title → `attention/waiting`，显示“等待输入”。
- `shellForeground === true` → 不能判为 idle。
- 无 live PTY 的旧 idle 标题 → 不能判为 current。
- retained done → 只能作为 completed 来源。
- title-derived synthetic 状态词不成为 Provider 内容。
- 本机、SSH、runtime host 的证据路径。
- freshness epoch 后重新分类。
- lifecycle 在超过 20 次状态切换、`stateHistory` 裁剪后保持不变。
- working / waiting / blocked / done / idle 与 Prompt 切换不重置 lifecycle。
- reducer 对同一事件幂等，并拒绝旧 `authorityRevision` 的迟到事件。
- PTY 替换、Pane retire、confirmed shell、Agent 类型变化、Provider Session 变化和 launch token 变化会重置 lifecycle。
- `agent === null && shellForeground === false` 的不确定探测不重置 lifecycle。
- Provider Session / launch token 从空变为首次非空时只补充，不重置 lifecycle。
- authority transfer 在同 Host / PTY authority 下原样移动 identity；Host、PTY 冲突时不合并。
- title-derived / SSH Agent 在同一 live PTY 状态切换中保留 lifecycle。
- SSH 临时断连标记 transport-disconnected，不能证明 current 或生成导航目标。
- SSH 重连只有在 Host、connection、PTY / relay authority 连续时保留 ID；无法证明时生成新 ID。
- retained 原样复制 lifecycle ID、startedAt 和 Host，不得成为 identity 写入者。

### 17.2 Agent Activity 模型

至少覆盖：

- current idle 携带最近真实结论。
- 同 Session idle 与 completed 去重。
- current working / attention 全量保留。
- idle 前 3 条折叠与完整计数。
- completed 独立限制为 3 条。
- 排序优先级稳定。
- live / retained 去重。
- Provider Session 精确匹配。
- `codexdb` / `codexdba` 归一为 Vault `codex`，运行态标签保持原值。
- Pane 被新 Provider Session 复用时，旧 retained 不成为 current。
- 无 Provider Session 时使用不可变 `agentLifecycleId` 构建 lifecycle canonical key。
- current 与 completed 的 item ID 不同时，仍按 identity aliases 交集去重。
- completed item 的 `completedAt` 只影响 item ID，不影响 Session 互斥。
- Provider Session 晚到时，Provider key 成为 canonical key，lifecycle alias 仍能抑制同一 Session 的 completed。
- lifecycle 重置后，相同 Pane 和 Agent 类型生成不同 canonical key。
- aliases 集合至少包含 canonical key，并按集合交集而非单字段相等判重。
- NUL tuple 编码区分字段边界；组件含 NUL 时拒绝构建 key。
- 本机与 SSH、两个 SSH Host、runtime host 上相同 Agent + Provider Session ID 分别匹配。
- 缺少 lifecycle 的 legacy retained 只能显示为不可导航 completed。
- interrupted done 不进入 completed。
- 空白结论不进入 completed。
- workspace/project、host、Agent 和搜索过滤。
- folder workspace 和 runtime-owned folder workspace。

### 17.3 安全导航

至少覆盖：

- 当前 working / attention / idle 可激活 worktree、tab 和 Pane。
- worktree 已删除时不跳转。
- tab 已关闭时不跳转。
- leaf 已变化时不跳转。
- Provider 三元组任一字段不一致时不跳转。
- 本机与 SSH Host 上存在相同 Agent + Provider Session ID 时只跳到 target Host。
- 两个 SSH Host 上存在相同 Agent + Provider Session ID 时不串 Pane。
- Host 无法解析、被切换或候选跨 Host 歧义时不生成 target / 不跳转。
- Pane 被复用时不跳到新 Session。
- 无 Provider Session 且 `agentLifecycleId` 变化时不跳转。
- lifecycle 缺失时不生成 current 导航目标。
- 仅有 transport-disconnected lifecycle 时不生成 current 或导航目标；另有独立 fresh current 证据时可展示但仍无导航目标。
- idle 在点击前已回到 shell 时不跳转。
- completed 仅在精确匹配时提供 Open pane。
- 可用 Session log 正确调用 `openAiVaultSessionLogInOrca()`。

### 17.4 组件测试

至少覆盖：

- 无数据时整个区域隐藏。
- 标题为“Agent 动态”。
- 非零 attention / working / idle 统计。
- 状态文字和无障碍标签。
- working 显示工具步骤。
- waiting / blocked 显示真实待处理摘要。
- idle 显示最近结论或“已打开，当前空闲”。
- idle 超过 3 条可展开、收起。
- current 行主体执行安全聚焦。
- `navigationTarget === null` 的 current 状态仍展示，但不是 button、不可点击、不进入 tab order，并显示无法定位原因。
- completed 展开、复制、打开日志。
- 不可用动作不显示。
- 键盘操作和 focus 行为。

### 17.5 回归测试

- 左侧 Worktree Agent 行的状态、排序和展开不回退。
- Agent Dashboard counts 与卡片状态一致。
- folder workspace 只进入右侧 Agent 动态，不产生伪造 `DashboardCard`。
- AI Vault 原有 Session 虚拟列表、搜索和 Resume 不回退。
- `codex`、`codexdb`、`codexdba` 的图标与 Vault 匹配不回退。
- Host 分组和 folder workspace 归属不回退。

## 18. 实施顺序

1. 将评审中的失败用例先写成测试，确认旧实现失败。
2. 增加不可裁剪的 Agent lifecycle identity、唯一事件 reducer、authority revision、防迟到事件、retained 原样传递和完整事件表测试。
3. 抽取或补齐共享 Pane current-presence 证据模型，并统一 title permission → waiting。
4. 扩展现有 repo Dashboard snapshot，接入 `paneForegroundAgentByPaneKey`、lifecycle map 并保留来源证据；不把 folder workspace 伪装成 DashboardCard。
5. 为右侧 Agent 动态覆盖普通 worktree、folder workspace 和 runtime-owned folder workspace。
6. 集中实现 Provider 三元组、NUL tuple 编码、canonical key 和 aliases 集合，禁止通过 item ID 或裸 session ID 做 Session 去重。
7. 新建 `AgentActivityItem` 纯模型，完成分类、互斥、Session 匹配、过滤、排序和完整计数。
8. 升级原 Pane 索引和导航 target，完成 Host / Pane / Provider 三元组 / lifecycle 二次校验及跨 Host 同 ID 测试。
9. 将 `AgentConclusionsBox` 替换为 `AgentActivityBox` 及具体行组件。
10. 接入 `AiVaultPanel`，完成当前项导航与 completed 动作。
11. 拆分超限文件，消除所有本功能相关 `max-lines` 错误。
12. 运行定向测试、全量类型检查、lint 和 diff 检查。
13. 在不影响用户现有 Orca 实例的前提下，另行完成浅色/深色、本机/SSH 手工验收。

## 19. 验收标准

以下条件必须全部满足，才可判定通过：

1. 右侧区域标题为“Agent 动态”，不再显示“Recent agent conclusions”。
2. fresh working、waiting、blocked 能进入右侧区域。
3. title-derived permission 在所有表面统一映射为 waiting，并显示“等待输入”。
4. 前台 Agent 或 live PTY idle 能显示为“已打开 · 当前空闲”。
5. retained done 不会被误判为 idle。
6. stale working 无当前存在证据时不再显示为运行或空闲。
7. `agentLifecycleId` 由唯一 reducer 写入，不受状态历史裁剪影响，并按事件表在 Pane / PTY / Host / Agent 生命周期边界保留、转移或重置。
8. retained 只原样复制 lifecycle identity；SSH 断连、重连和 authority transfer 不会错误复用或重置 identity。
9. current 与 completed 使用 canonical key + aliases 集合互斥，不能依赖展示 item ID 相等。
10. identity key 使用集中定义的带类型 NUL tuple 编码，不存在字段拼接碰撞。
11. 无 Provider Session 时，Pane 复用可通过 lifecycle identity 被可靠识别。
12. Provider 匹配和原 Pane 导航严格校验 `executionHostId + normalizedVaultAgent + providerSessionId`，跨 Host 同 ID 不会串 Pane。
13. current 行有安全 target 时可聚焦原 Pane；无 target 时仍展示状态但不伪装成可点击控件，并说明原因。
14. Pane / Session / lifecycle 复用时拒绝错误跳转。
15. completed 支持展开、复制，并在可用时打开 Session log。
16. attention、working 不截断；idle 可展开；completed 独立保留最近 3 条。
17. 标题统计来自完整过滤结果，不受显示上限影响。
18. 普通 worktree、folder workspace、SSH 和 runtime host 均有测试；folder workspace 不被伪造成 DashboardCard。
19. 状态有可见文字和无障碍标签。
20. 不新增 hardcoded color、模糊模块名或 `max-lines` disable。
21. 以下验证全部通过：
    - 相关 Vitest 定向测试。
    - `pnpm typecheck`。
    - `pnpm lint`。
    - `git diff --check`。
22. 最终汇报明确列出：
    - 状态模型。
    - idle 权威证据。
    - lifecycle identity 的创建与重置规则。
    - canonical key、aliases 与 item ID 的职责边界。
    - Provider 三元组和跨 Host 导航校验。
    - 去重和 Pane 复用策略。
    - 修改文件。
    - 测试命令与结果。
    - 未完成的手工验收和已知限制。

## 20. 评审意见关闭映射

| 评审意见 | SPEC 对应章节 | 关闭条件 |
|---|---|---|
| P0：当前 Agent 完全不会展示 | 7、8、9 | 四类互斥模型和 current-first 构建完成 |
| P0：idle 未复用左侧权威证据 | 5、6、9 | 左侧、Dashboard、右侧复用同一证据链 |
| P0：没有回到 Agent / Open log | 12、13 | 当前项安全导航和 completed 动作通过测试 |
| P1：标题、统计、排序、截断不符合 | 13 | 完整统计、固定排序、独立上限和展开完成 |
| P1：Pane / Session 复用去重不足 | 8、11、12 | current-first、Provider Session、lifecycle identity 和 aliases 集合校验完成 |
| P1：测试计划未落实 | 17、19 | 新增失败用例和所有门禁通过 |
| lint：多个 max-lines 失败 | 14、15 | 职责拆分后 `pnpm lint` 通过 |
| 复评 P0：stateHistory 不能提供稳定 Session 起点 | 6.4、8、12 | 独立不可裁剪 lifecycle identity 及重置测试完成 |
| 复评 P1：title permission 映射不明确 | 7.2、7.3、13 | permission 全链路归一为 waiting |
| 复评 P1：无 Provider Session 去重缺少 Session key | 8.3、11 | canonical key + aliases 与 item ID 分离并完成互斥测试 |
| 二次复评 P0：跨 Host 同 Provider ID 可能跳错 Pane | 10.1、10.2、12 | Provider 三元组索引、target 携带 Host、点击时 Host 二次校验和跨 Host 同 ID 测试完成 |
| 二次复评 P0：lifecycle 写入所有者不明确 | 6.4、14、17.1 | 唯一 slice/reducer、完整事件表、迟到事件保护和 retained 原样复制测试完成 |
| 二次复评 P1：Session key 与 alias 模型不明确 | 8.3、11 | canonical key + aliases 集合、集中 NUL tuple 编码和碰撞测试完成 |
| 二次复评 P1：folder workspace 的 Dashboard 范围不明 | 9.2、9.3、17.5 | folder 进入右侧 Agent 动态但不伪造 DashboardCard |
| 二次复评 P1：无导航目标的 current 交互矛盾 | 12.3、13.5、17.4 | current 仍展示，非交互且说明不可导航原因 |
