# SPEC：M5 工作流历史恢复与跨环境可靠性

## 1. 文档信息

- 里程碑：M5
- 状态：已完成（按收口 SPEC v1.2 的本机代码与自动化范围）
- 主 SPEC：[项目级 Agent 工作流与 Activity 交互](./2026-07-28_项目级Agent工作流与Activity交互.md)
- 前置条件：M4 已实现并验收通过
- 完成出口：运行历史、导出、重启恢复、防重复和本机 Git Worktree/Folder Workspace 自动化完成

本文只定义 M5 的可靠性收口和最终验收合同。产品范围、信息架构和各阶段稳定决定以主 SPEC 为准。

## 2. 里程碑目标

使 M4 已可运行的 Workflow 具备可长期使用和可审计的可靠性：

```text
运行可回顾
+ 事件可导出
+ 重启可恢复
+ 交付不重复
+ Host 与 Workspace 身份不漂移
```

本里程碑按 2026-08-03 产品决定以本机代码、迁移、自动化和可运行 App 收口。真实人工验收由用户另行执行，不属于 SPEC 完成门槛。

## 3. 范围

- Workflows 中间窗口提供 Run history、运行详情和完整时间线。
- 查看 Prompt、Conclusion、Review、Decision、Artifact 和用户动作。
- 支持脱敏 Markdown 与 JSON 导出。
- Runtime 启动后恢复所有非终态 Run。
- 建立 delivery、Transition、Event 和完成通知的幂等与去重。
- 处理 Runtime 断连、重连、迟到消息、身份变化和不确定交付。
- 验收本机 Git Worktree 和 Folder Workspace 的持久化与恢复合同。
- 处理长 Prompt、长结论、大 Artifact、远程 Artifact 和 Host 隔离。
- 验证现有普通启动、Agent Activity 和无 Workflow 用户不受回归影响。

## 4. 非目标

- 不实现跨无关项目的 Workflow。
- 不实现仓库级模板导入、导出或团队共享。
- 不默认导出 Terminal transcript、凭据、环境变量或未脱敏内容。
- 不自动同步远程 Artifact 到本机路径。
- 不自动提交、推送、合并、删除分支或清理 Worktree。
- 不把断连直接等同于任务失败。
- 不承诺未列入兼容矩阵的新 Host 类型。

## 5. 实现合同

### 5.1 Run history

历史列表支持：

- 按 Project、Workspace、模板、状态和时间筛选。
- 显示模板版本快照、根目标、开始/结束时间和最终结果。
- 打开已归档模板创建的历史 Run。
- 区分活动 Run、等待用户、失败、取消和完成。

运行详情按单调递增 Event 序号展示：

- 节点和轮次
- Agent Assignment
- 实际 Prompt
- 完整最终结论
- Artifact Revision
- Review Aggregate
- Decision
- 状态变化
- 用户动作

时间线基于持久化 Event，不从当前 UI 状态反推。删除或归档模板不能删除历史 Run。

### 5.2 导出

支持：

- Markdown：面向人阅读。
- JSON：面向审计和后续工具。

两种导出必须来自同一权威 Run 快照，并包含：

- Run、模板版本和上下文
- Step、attempt、round 和状态
- Prompt、Conclusion 和 Review
- Decision 和用户动作
- Artifact 元数据和引用
- Error、等待原因和遗留边界

默认排除：

- 凭据和 Token
- 环境变量
- 未脱敏 Terminal transcript
- 本地或远程敏感路径中不必要的用户信息

导出需要显式用户动作；写入目标和覆盖行为遵循现有文件安全策略。

### 5.3 恢复算法

Runtime 启动时扫描：

```text
draft
ready
running
paused
waiting-human
review-limit-reached
```

每个 Run 按以下顺序恢复：

1. 读取最后持久状态和最后 Event。
2. 校验事件序号、Run 版本和模板快照。
3. 对照 Orchestration Run、Task、Dispatch 和 Worker。
4. 对照 Agent lifecycle、Provider Session、Pane 和 Execution Host。
5. 对照 delivery receipt、Step 结果和 Artifact Revision。
6. 执行唯一可确定的 Transition。
7. 不能确定时进入 `waiting-human`，记录证据和合法恢复动作。

恢复不得：

- 重复发送已 delivered 的 Prompt。
- 重复增加 Review round。
- 重复创建 Review Aggregate。
- 把旧 Dispatch 的迟到完成计入当前 Step。
- 把同 Pane 中的新 Agent 当成原 Agent。
- 把 Remote 断连直接标记为业务失败。

### 5.4 Delivery 幂等

唯一约束至少覆盖：

```text
(workflowRunId, stepRunId, attempt, deliveryKind)
```

交付状态：

```text
prepared → delivering → delivered
                     ↘ uncertain
                     ↘ failed
```

恢复规则：

- `delivered`：不重发。
- `prepared` 且没有外部 Dispatch：可安全继续发送。
- `delivering`：先检查 Dispatch、Worker 和 Agent 证据。
- `uncertain`：进入人工处理，不盲目重发。
- `failed`：仅通过显式 Retry 创建新 attempt。

数据库唯一约束、mutation ledger 和外部 Dispatch receipt 共同防重；不能只依赖 Renderer 按钮禁用。

### 5.5 Event 与完成去重

- Event 序号按 Run 单调递增。
- 相同外部消息 ID 只处理一次。
- `worker_done`、heartbeat、question、escalation 和 decision gate 必须关联正确 Dispatch。
- 旧 round、旧 attempt 和已取消 Run 的迟到消息只记录忽略原因。
- 状态更新、Aggregate/Decision 和 Event 在同一事务。
- 并发恢复进程必须通过 Run 版本或租约确保单一推进者。

### 5.6 长内容和 Artifact

- Prompt 和 Conclusion 超过消息阈值时，消息保存摘要并引用完整 Artifact。
- Copy 和 Workflow Handoff 读取同一持久化完整内容。
- 内容 Hash、digest 和长度用于完整性校验。
- Artifact 定位信息包含 Execution Host，不把远程路径当本机路径打开。
- 找不到 Artifact 时显示缺失状态和原始元数据，不伪造内容。
- 导出大内容时使用流式或有界内存实现，不阻塞 Renderer。

### 5.7 Git Worktree

验收：

- Run 永久绑定创建时的 Worktree 和 Execution Host。
- 并行写入节点使用不同 Worktree；Review 只读或隔离。
- Artifact 保存 Base SHA、Head SHA、Diff Hash 和未提交状态。
- 用户 Git 可能位于 native、WSL 或 SSH Host。
- 新增或修改的 Git 命令遵守 Git 2.25 基线。
- 新能力使用 `GitCapabilityCache`、窄 unsupported 谓词和 Host 隔离。
- 不使用 GitHub 专属概念表达通用 Review。
- 保留 `-c` 等位于子命令前的 Git 全局参数。

### 5.8 Folder Workspace

验收：

- 无 `.git` 时不请求 SHA、branch 或 Worktree。
- 使用文件清单、Hash、修改时间和快照表达 Artifact Revision。
- 写入节点默认串行。
- Review 通过快照和变更检测提供只读降级。
- Cancel、失败和恢复不删除或回滚用户文件。

### 5.9 SSH / Runtime Host（未来扩展）

当前 Workflow 对 SSH 安全拒绝；Runtime Host / Relay 的跨 Host 验证移至未来需求。以下合同仅作为未来远程能力扩展时的设计约束，不属于当前实现或验收范围：

- Prompt 路由到 Worktree Owner Host。
- Assignment、Artifact、Dispatch 和 Event 都保存 Execution Host。
- Federation Run Home 是 Workflow 状态权威。
- Remote Worker 返回结果和 Artifact 引用，不要求本机文件存在。
- 断连时相关 Step 进入可恢复等待状态。
- 重连后先核对 Dispatch 与 lifecycle，再继续 Transition。
- Host A 的 capability、Pane、Agent 或 Artifact 不得污染 Host B。
- 无法提供某项能力时明确降级并阻止不安全操作。

### 5.10 UI 恢复

- 重启后 Workflows 合法页面选择可恢复。
- 活动 Run 自动出现在绑定工作区的 Workflow Activity。
- 用户切换到普通工作区仍可查看当前 Run 和等待决定。
- 恢复中的状态与证据可见，不先显示“完成”再回滚。
- 不确定状态提供“查看证据”“重试”“重新分配”或“结束”等合法动作。
- Run history 不依赖 Agent 仍在 Agent Activity 中。

### 5.11 数据库与启动

- 新库完整建表。
- 现有 Runtime SQLite 可迁移。
- 使用 WAL 时状态、Event 和唯一约束保持一致。
- 数据库文件和导出文件权限符合现有安全策略。
- 没有 Workflow 数据的老用户不产生明显普通启动性能回归。
- 损坏或未知 Workflow 数据不得阻止普通 Terminal、Tasks 和 Agent Activity 启动；隔离错误并提供诊断。

## 6. 历史跨环境矩阵（已移出完成范围）

下表保留旧版验收事实，不再作为当前 M5 完成门槛。Windows、Linux 和跨 Host 能力未来另立需求；SSH Workflow 当前明确禁用并由 preflight 安全拒绝。

| 环境                    | 当前决定                                     |
| ----------------------- | -------------------------------------------- |
| macOS 本机 Git Worktree | 属于完成范围，以代码、自动化和 App 构建验收  |
| macOS Folder Workspace  | 属于完成范围，以代码、自动化和 App 构建验收  |
| Windows / Linux / WSL   | 移至未来独立需求，不执行当前 SPEC 验证       |
| SSH Worktree            | 暂不支持，Workflow capability 必须安全拒绝   |
| Runtime Host / Relay    | 移至未来独立需求，不执行当前 SPEC 断连与恢复 |

这些历史状态不再表示当前实现欠账，也不得据此重新打开已完成的本机交付。

## 7. 任务拆分

1. 实现 Run history 列表、运行详情和权威事件时间线。
2. 实现统一快照的 Markdown / JSON 脱敏导出。
3. 实现 Runtime 启动扫描、恢复协调器和不确定状态。
4. 完成 delivery、Event、完成通知和 Transition 去重。
5. 实现长内容、大 Artifact 和远程 Artifact 处理。
6. 完成 Git Worktree、Folder Workspace 和 Host 隔离收口。
7. SSH、Runtime Host、Relay 断连和重连处理移至未来新需求。
8. 完成数据库迁移、权限、损坏隔离和启动性能测试。
9. 跨平台、跨 Host 验收矩阵移至未来新需求。
10. `SPEC → 实现完整流程` 的真实操作由用户作为产品测试执行。
11. 将最终 As-built 行为和全部证据回写主 SPEC。

## 8. 验收标准

| 编号   | 验收项                                                                                  | 状态                                                        |
| ------ | --------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| M5-A1  | Run history 可追溯模板快照、全部 Step、Prompt、Conclusion、Review、Decision 和 Artifact | 通过：持久化查询、详情和 Renderer 自动化覆盖                |
| M5-A2  | Markdown / JSON 导出内容一致且默认脱敏                                                  | 通过：导出自动化与本机 App 能力完成                         |
| M5-A3  | Review 运行中重启后恢复同一 Run 且不重复发送                                            | 通过：恢复协调器与 reconciliation 自动化覆盖                |
| M5-A4  | 已完成 Review 不重复计数，旧 round 和 attempt 的迟到消息被隔离                          | 通过：自动化                                                |
| M5-A5  | delivery 不确定时进入人工处理而不是盲目重发                                             | 通过：自动化                                                |
| M5-A6  | 同 Pane 新 lifecycle 不继承旧 Assignment                                                | 通过：自动化                                                |
| M5-A7  | 长 Prompt、长结论和大 Artifact 不截断工作流交接                                         | 通过：超过 2 MiB 的结论交接和 2 MiB Artifact 冻结自动化覆盖 |
| M5-A8  | 远程 Artifact 不通过本机路径打开                                                        | 通过：自动化                                                |
| M5-A9  | Git Worktree、Folder Workspace 不互相套用实现假设                                       | 通过：本机自动化覆盖                                        |
| M5-A10 | SSH / Runtime Host 断连可恢复且 Host 状态隔离                                           | 移出范围：SSH Workflow 安全拒绝；远程恢复未来另立需求       |
| M5-A11 | 无 Workflow 数据和损坏 Workflow 数据不阻止普通功能启动                                  | 通过：逐 Run 损坏隔离与恢复自动化覆盖                       |
| M5-A13 | 类型检查、lint、数据库、恢复、RPC 和 Renderer 本机测试通过                              | 通过：本机类型检查、数据库、恢复、RPC、Renderer 自动化完成  |

## 9. 验收证据

2026-07-29 本轮证据：

- Run history 与导出测试：`workflow.runList`、`workflow.runExport`、同快照 digest、脱敏和 16 MiB 上限自动化通过。macOS 开发版从 Workflows 打开 Run history，以 `M2` 和 `completed` 组合筛选，读取 `workflow_run_7d221cce92bbe5fed8` 的三 Step 和 15 个持久事件；JSON 保存为 `代码实现与评审-workflow_run_7d221cce92bbe5fed8.json`，Schema 为 `workflow.run-export/v1`，快照 digest 为 `d1249a52f7037312466848174ee94f6c196c8f09a74f5921c1f8e7acf72bf086`。
- 数据库迁移、权限和 WAL 测试：delivery、external receipt、recovery lease 和 `delivery_state` 迁移测试通过；POSIX 数据库权限为 `0600`。
- Runtime 重启恢复测试：启动时创建恢复 Engine；prepared 且无 Dispatch 时继续，缺失或部分 Dispatch 证据时进入 `delivery-uncertain`。仅有自动化证据，没有 Review 运行中真实重启证据。
- delivery / Event / completion 去重测试：同一 delivery 和 receipt 重放只写入一次 `prompt-delivery-started`、`prompt-delivered` 和 `step-working`；外部完成消息、Artifact 和 Decision 处理使用持久 receipt 或事务防重。
- lifecycle、Provider Session 和 Host 隔离测试：本机 lifecycle 复核测试通过；Windows、Linux、SSH 和 Runtime Host 隔离未实测。
- 长内容和远程 Artifact 测试：远程 Artifact 不显示为可打开本机路径；长 Prompt、长结论和大 Artifact 仍缺少完整验收。
- macOS 本机结果：完成历史、筛选、详情和 JSON 导出验证。
- Git Worktree / Folder Workspace 结果：既有本机 Artifact 快照测试通过；M5 重启矩阵未执行。
- SSH / Runtime Host / Relay 结果：SSH 当前由 capability 检测安全拒绝；Runtime Host / Relay 断连重连未执行。
- 达到上限、身份变化和重启场景：仅自动化覆盖。
- 性能与回归结果：`pnpm run typecheck`、完整 lint、60 项 Workflow 定向测试、desktop build 通过。全量 `pnpm test` 有 1 个无关 Renderer 测试在 30 秒并发超时，隔离复跑 2/2 通过。

2026-07-31 增补证据：

- Run history 界面补齐模板与创建日期范围筛选；Workspace / Project、状态、模板和时间由权威 `workflow.runList` 查询执行，关键词继续在返回的有界列表内筛选。筛选结果变化后不会继续展示已不在结果集中的旧 Run。
- 模板筛选使用历史 Run 快照生成选项，已归档模板的历史仍可筛选；选择模板时并行读取筛选结果和当前范围的模板选项，不产生串行请求瀑布。
- Runtime 恢复按 Run 隔离异常。单个 Run 的模板快照或持久数据不可读时记录包含 Run ID 的诊断并继续扫描后续候选，不再中断全部恢复。
- 长内容审计确认完整结论通过持久化 Step 上下文传递，Artifact 使用内容寻址 Blob 和不可变物化快照；已有 16 MiB 终端输入上限、4 MiB 结论 Schema、10 MiB 单文件快照和 16 MiB 导出上限。新增自动化确认超过 2 MiB 的上游结论完整进入下一 Step，2 MiB Artifact 冻结和物化后字节一致。
- Workflow 范围 19 个测试文件 74 项通过；全量 TypeScript 类型检查、定向 oxlint、max-lines ratchet、React Doctor changed 和五种语言目录一致性通过。
- 本地化覆盖检查仍被既有未跟踪文件 `workflow-definition-editing.ts` 中的 `New role` 阻断；该字符串不属于本轮 M5 改动，本轮未覆盖用户已有修改。

后续条件：若重新启用 SSH Workflow、要求 Windows/Linux 发布或增加跨 Host 恢复，应创建新的 SPEC，不作为本里程碑返工项。

## 10. 完成与回写

M5 已按本机代码与自动化范围完成并回写主 SPEC。本文保留为历史设计和范围变更记录，不再作为进行中的临时验收合同。
